# Description: Counts 444 responses, which nginx only returns where your own configuration says to
# Note this bans on a judgement your vhost already made, so the rules live there rather than here
[Definition]
failregex = ^<HOST>.*" 444\s
ignoreregex =
