# Description: Bans clients that nginx has already refused under one of its rate limiting zones
# Note this reads the nginx error log, where the limiting is recorded, rather than the access log
[Definition]
failregex = ^[^#]*#0: \*[\d]* limiting (?:requests, excess: [\d\.]+|connections) by zone "[^"]*", client: <HOST>,
ignoreregex = 